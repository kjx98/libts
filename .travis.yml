dist: xenial
compiler: gcc
addons:
  apt:
    packages:
    - git
    - cmake
    - ccache
    - g++

branches:
  only:
  - master

before_install:
  - sudo apt-get update -q

install:
  - mkdir build

script:
  - cd build
  - cmake ../tests
  - make; make test
